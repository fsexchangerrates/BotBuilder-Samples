const { CardAction, CardFactory, MessageFactory, ActionTypes, ActivityHandler } = require('botbuilder');
class FlexMessageCards implements AttachmentsBot {
    constructor(MessageFactory) {
        
    }
}